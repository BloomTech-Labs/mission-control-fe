const initialState = {
    persons: [],
    person: {}
};

export const personStore = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};