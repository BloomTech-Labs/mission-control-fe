import { IntrospectionQuery } from 'graphql';
import { Exchange } from 'urql/core';
import { UpdatesConfig, ResolverConfig, OptimisticMutationConfig, KeyingConfig, StorageAdapter } from './types';
export interface CacheExchangeOpts {
    updates?: Partial<UpdatesConfig>;
    resolvers?: ResolverConfig;
    optimistic?: OptimisticMutationConfig;
    keys?: KeyingConfig;
    schema?: IntrospectionQuery;
    storage?: StorageAdapter;
}
export declare const cacheExchange: (opts?: CacheExchangeOpts | undefined) => Exchange;
